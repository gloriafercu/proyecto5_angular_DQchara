import { LimitLongTextPipe } from './limit-long-text.pipe';

describe('LimitLongTextPipe', () => {
  it('create an instance', () => {
    const pipe = new LimitLongTextPipe();
    expect(pipe).toBeTruthy();
  });
});
